

export default function FullName (props){
    const fullname = props.fullname;
    return(
        <div>
            <h2><strong className="h2">Full Name:</strong> {fullname}</h2>
        </div>
    );

    
}

